# Run `pre-commit install` once after cloning.
# submission/ is excluded everywhere: it is a frozen record and any hook that rewrites bytes
# there would invalidate the hashes in RESULTS.md.
exclude: '^submission/'

repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-toml
      - id: check-json
      - id: check-merge-conflict
      - id: check-added-large-files
        args: ['--maxkb=512']
      - id: detect-private-key
      - id: mixed-line-ending
        args: ['--fix=lf']

  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.8.4
    hooks:
      - id: ruff
        args: [--fix]
      - id: ruff-format

  - repo: local
    hooks:
      - id: scrub-check
        name: scrub check (no secrets, no private identifiers)
        entry: scripts/scrub_check.sh
        language: script
        pass_filenames: false
        always_run: true
