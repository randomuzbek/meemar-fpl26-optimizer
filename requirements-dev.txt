# Development and CI dependencies. Deliberately tiny: the tests must run on a machine with no
# Vivado, no RapidWright and no FPGA, so nothing here pulls in the toolchain.
pytest>=9.1.1
ruff>=0.16.5
pre-commit>=4.6.2
